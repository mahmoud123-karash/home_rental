import 'package:flutter/material.dart';

class ClientNavBarView extends StatelessWidget {
  const ClientNavBarView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
